#include "Daemon.hpp"
#include <iostream>
#include <chrono>

// Daemon::Daemon(HardwareSerial& serial) : _requestHandler(serial) {
//   _requestHandler.printAvailableCommands();
//   _requestHandler.printAvailableForDataFrame();
// }

// void Daemon::start() {
//   const auto batchSize = 10;
//   while (true) {
//     const auto now = std::chrono::system_clock::now();
//     for (int i = 0; i < batchSize; i++) {
//       const auto dataFrame = _requestHandler.getDataFrame();
//       std::cout << dataFrame.toString() << "\n\n";
//     }
//     const auto end = std::chrono::system_clock::now();
//     std::chrono::duration<double> elapsedSeconds = end - now;
//     std::cout << "Batch took: " << elapsedSeconds.count() << " seconds\n";
//     std::cout << "Frequency: " << batchSize / elapsedSeconds.count() << " Hz\n\n";
//   }
// }
